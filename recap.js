// function dam(price) {
//     const ret = price * 5;
//     return ret;
// }

// function add(price1, price2) {
//     const jog = price1 + price2;
//     console.log(jog);
// }
// // const price = dam(300);
// // console.log(price);
// // add(price, 600);

// function add(price, price1, price2) {
//     return (price + price1) * price2;
// }

// const total = add(20, 20, 10);
// // console.log(total);

function doMath(num1, num2) {
    const sum = num1 + num2;
    const diff = num1 - num2;
    const multiply = sum * diff;
    const results = multiply / 2;
    return results;
}

const totale = doMath(10, 5);
console.log(totale);
